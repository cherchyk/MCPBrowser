name: Deploy to npm and VS Code Marketplace

on:
  push:

permissions:
  contents: write
  id-token: write  # Required for MCP Registry OIDC authentication
    
jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'
    
    - name: Install dependencies
      run: |
        npm ci
        cd MCPBrowser && npm install
        cd ../VSCodeExtension && npm install
    
    - name: Run tests (skip manual auth tests)
      run: npm run test:unit

  deploy:
    runs-on: ubuntu-latest
    needs: test
    if: always() && needs.test.result == 'success'
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        registry-url: 'https://registry.npmjs.org'
    
    - name: Install dependencies
      run: |
        npm ci
        cd MCPBrowser && npm install
        cd ../VSCodeExtension && npm install
    
    - name: Install vsce
      run: npm install -g @vscode/vsce
    
    - name: Verify version synchronization
      id: version
      run: |
        MCP_VERSION=$(node -p "require('./MCPBrowser/package.json').version")
        EXT_VERSION=$(node -p "require('./VSCodeExtension/package.json').version")
        SERVER_VERSION=$(node -p "require('./server.json').version")
        
        if [ "$MCP_VERSION" != "$EXT_VERSION" ] || [ "$MCP_VERSION" != "$SERVER_VERSION" ]; then
          echo "::error::Version mismatch detected!"
          exit 1
        fi
        
        echo "✅ All versions synchronized at $MCP_VERSION"
        echo "version=$MCP_VERSION" >> $GITHUB_OUTPUT
    
    - name: Verify npm token (dry-run)
      run: |
        cd MCPBrowser
        npm whoami
      env:
        NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
    
    - name: Verify VS Code token
      run: |
        npm install -g @vscode/vsce
        vsce --version
      env:
        VSCE_PAT: ${{ secrets.VSCE_PAT }}
    
    - name: Publish to npm
      if: github.ref == 'refs/heads/main'
      run: |
        cd MCPBrowser
        npm publish
      env:
        NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
    
    - name: Skip npm publish (not on main branch)
      if: github.ref != 'refs/heads/main'
      run: |
        echo "⏭️ Skipping npm publish - not on main branch"
        echo "Branch: ${{ github.ref }}"

    - name: Publish to MCP Registry
      if: github.ref == 'refs/heads/main'
      run: |
        # Install mcp-publisher
        curl -L "https://github.com/modelcontextprotocol/registry/releases/latest/download/mcp-publisher_linux_amd64.tar.gz" | tar xz mcp-publisher
        
        # Authenticate using GitHub OIDC (no secrets needed!)
        ./mcp-publisher login github-oidc
        
        # Publish to MCP Registry
        ./mcp-publisher publish
    
    - name: Skip MCP Registry publish (not on main branch)
      if: github.ref != 'refs/heads/main'
      run: |
        echo "⏭️ Skipping MCP Registry publish - not on main branch"
    
    - name: Package VS Code Extension
      run: |
        cd VSCodeExtension
        vsce package --no-dependencies
    
    - name: Publish to VS Code Marketplace
      if: github.ref == 'refs/heads/main'
      run: |
        cd VSCodeExtension
        vsce publish --packagePath mcpbrowser-${{ steps.version.outputs.version }}.vsix
      env:
        VSCE_PAT: ${{ secrets.VSCE_PAT }}
    
    - name: Skip VS Code publish (not on main branch)
      if: github.ref != 'refs/heads/main'
      run: |
        echo "⏭️ Skipping VS Code publish - not on main branch"
        echo "Branch: ${{ github.ref }}"
    
    - name: Extract changelog for version
      id: changelog
      run: |
        VERSION=${{ steps.version.outputs.version }}
        
        # Extract changelog section for this version
        # This is a simple extraction - adjust regex based on your CHANGELOG format
        CHANGELOG=$(awk "/## \[$VERSION\]/,/## \[/ {print}" CHANGELOG.md | head -n -1)
        
        # If extraction failed, use a default message
        if [ -z "$CHANGELOG" ]; then
          CHANGELOG="Release version $VERSION"
        fi
        
        # Save to file for multi-line output
        echo "$CHANGELOG" > release-notes.md
    
    - name: Create GitHub Release
      if: github.ref == 'refs/heads/main'
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        VERSION=${{ steps.version.outputs.version }}
        
        # Create release with changelog
        gh release create "v$VERSION" \
          --title "v$VERSION" \
          --notes-file release-notes.md \
          VSCodeExtension/mcpbrowser-$VERSION.vsix
    
    - name: Skip GitHub Release (not on main branch)
      if: github.ref != 'refs/heads/main'
      run: |
        echo "⏭️ Skipping GitHub Release - not on main branch"
        echo "Branch: ${{ github.ref }}"
    
    - name: Deployment Summary
      run: |
        VERSION=${{ steps.version.outputs.version }}
        if [ "${{ github.ref }}" == "refs/heads/main" ]; then
          echo "🎉 Successfully deployed version $VERSION to:"
          echo "  ✅ npm registry"
          echo "  ✅ MCP Registry (Visual Studio & other MCP clients)"
          echo "  ✅ VS Code Marketplace"
          echo "  ✅ GitHub Releases"
        else
          echo "✅ Pipeline test successful on branch: ${{ github.ref }}"
          echo "  ✅ Version check passed"
          echo "  ✅ Tests passed"
          echo "  ✅ npm token verified"
          echo "  ✅ VS Code token verified"
          echo "  ⏭️ Publishing skipped (not on main branch)"
        fi
