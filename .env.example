# Optional: Chrome remote debugging endpoint (otherwise auto-discovers/auto-launches)
CHROME_WS_ENDPOINT=

# Optional: Chrome remote debugging host/port used for discovery
CHROME_REMOTE_DEBUG_HOST=127.0.0.1
CHROME_REMOTE_DEBUG_PORT=9222

# Optional: Browser launch overrides if auto-launch is used
CHROME_PATH=
CHROME_USER_DATA_DIR=
